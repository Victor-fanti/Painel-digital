/*
 * ============================================================================
 * SIMULADOR FT DIGITAL - PROJETO FRONT-END
 * ============================================================================
 *
 * SOBRE O PROJETO:
 * Aplicação configurada com ferramentas modernas para garantir inicialização 
 * rápida e excelente experiência de desenvolvimento.
 * * TECNOLOGIAS UTILIZADAS:
 * - React: Biblioteca para a construção da interface (v18.3.1).
 * - JavaScript: Estruturado utilizando ESModules ("type": "module").
 * * ESTRUTURA E CONFIGURAÇÃO:
 * - Ponto de Entrada: A inicialização ocorre através do arquivo /src/main.jsx.
 * - Servidor Local: Configurado para rodar na porta 5173.
 * * COMO INSTALAR E EXECUTAR:
 * 1. Instale as dependências com: npm install
 * 2. Inicie o servidor com: npm run dev
 * 3. Acesse http://localhost:5173 no seu navegador.
 * * SCRIPTS DISPONÍVEIS (package.json):
 * - npm run dev     : Sobe o servidor Vite com hot-reload.
 * - npm run build   : Otimiza e empacota o código na pasta dist/.
 * - npm run preview : Inicia um servidor web para testar o build final.
 * * CONTROLE DE VERSÃO (.gitignore):
 * Ignora pastas de cache/build (node_modules/, dist/, .vite/),
 * arquivos de sistema (.DS_Store) e chaves sensíveis (.env).
 * ============================================================================
 */
